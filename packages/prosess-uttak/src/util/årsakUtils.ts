import Årsaker from '../constants/Årsaker';

// eslint-disable-next-line import/prefer-default-export
export const harÅrsak = (årsaker: Årsaker[], årsak: Årsaker): boolean => årsaker.includes(årsak);
