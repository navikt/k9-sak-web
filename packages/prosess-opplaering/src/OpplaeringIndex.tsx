import React from 'react';

interface OwnProps {
  placeholder: string;
}

const OpplaeringIndex = ({ placeholder }: OwnProps) => (
  <div>Et lite steg for k9-sak-web. Et stort steg for opplæringspenger</div>
);

export default OpplaeringIndex;
