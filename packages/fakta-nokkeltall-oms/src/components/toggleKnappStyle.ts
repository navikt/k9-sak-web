import { css } from 'styled-components';

const knappStyle = css`
  color: #0067c5;
  text-decoration: underline;
  padding: 0.2em;
  font-size: 0.9em;
  border: none;
  background-color: inherit;

  &:hover {
    cursor: pointer;
  }
`;

export default knappStyle;
