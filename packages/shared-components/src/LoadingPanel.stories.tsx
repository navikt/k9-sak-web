import React from 'react';

import LoadingPanel from './LoadingPanel';

export default {
  title: 'sharedComponents/LoadingPanel',
  component: LoadingPanel,
};

export const visPanel = () => <LoadingPanel />;
