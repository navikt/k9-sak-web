name: Valider pull request
on: [pull_request]
jobs:
  valid-pull-request:
    name: Valider pull request
    runs-on: ubuntu-latest
    steps:
      - name: Hente kode
        uses: actions/checkout@v2

      - name: Sette opp Node
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          registry-url: https://npm.pkg.github.com/
          scope: '@navikt'
          cache: 'yarn'

      - name: Cache Jest cache
        uses: actions/cache@v3
        id: cache-jest-cache
        with:
          path: jest_cache
          key: ${{ runner.os }}-${{ hashFiles('**/jest_cache') }}-jest
          restore-keys: |
            ${{ runner.os }}-

      - name: Installere dependencies
        run: yarn install --ignore-optional --no-progress --frozen-lockfile
        env:
          NODE_AUTH_TOKEN: ${{ secrets.READER_TOKEN }}

      - name: Tester, typescript-sjekk og linting
        run: yarn test:all --maxWorkers=2 && yarn ts-check && yarn less:lint
